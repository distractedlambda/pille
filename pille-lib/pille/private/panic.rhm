#lang pille/private/kernel

import:
  "cond.rhm" open

export fun panic() :: Never:
  cond
  | using_hosted_execution:
      rhombus:
        import rhombus/static open
        error("panic from Pille")
      prim.unreachable
  | ~else:
      prim.trap
