#lang rhombus/static/and_meta

import:
  "compiler.rhm" open

export class PerCompiler():
  private immutable key:
    Symbol.gen()

  method get_or_init(f :~ Function.of_arity(0)) :~ Any.like_result(f):
    Compiler
      .require_current()
      .get_or_init_attr(key, f)
