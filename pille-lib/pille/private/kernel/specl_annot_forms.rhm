#lang rhombus/static/and_meta

import:
  "specl_annot.rhm" open
  "specl_bind.rhm" open
  pille/concretize open

meta import:
  rhombus/static/meta open

meta export only_space namespace:
  specl_annot_meta

//==============================================================================
export specl_annot.macro '#%parens($child)':
  child

//==============================================================================
// Dummy binding for AfterInfixParsed usage
export specl_annot.macro '$lhs :: $_ ...':
  ~weaker_than: ~other
  ~associativity: ~none
  ~op_stx: op
  syntax_meta.error(op)

//==============================================================================
// Dummy binding for AfterInfixParsed usage
export specl_annot.macro '$lhs is_a $_ ...':
  ~order: equivalence
  ~associativity: ~none
  ~op_stx: op
  syntax_meta.error(op)

//==============================================================================
export specl_annot.macro '#%rhombus($(ann :: annot_meta.Parsed))':
  let (pred, _) = annot_meta.unpack_predicate(ann)
  specl_annot_meta.pack_rhombus_predicate(pred)

//==============================================================================
defn.macro 'rhombus_annots:
              $(pille_name :: Name): $(rhombus_ann :: Group)
              ...':
  '«specl_annot.macro '$pille_name':
      '#%rhombus($rhombus_ann)'
    ...»'

export rhombus_annots:
  alignment: satisfying(is_alignment)
  any: Any
  boolean: Boolean
  comparable: Comparable
  exact: Exact
  flonum: Flonum
  function: Function
  inexact: Inexact
  int: Int
  integral: Integral
  nat: Nat
  neg_int: NegInt
  neg_real: NegReal
  nonneg_real: NonnegReal
  number: Number
  pos_int: PosInt
  pos_real: PosReal
  rational: Rational
  real: Real
  string: String
  type: Type

fun is_alignment(n):
  guard.let n :: Int.in(0..0xFFFF_FFFF) = n | #false
  n == (1 bits.(<<) (bits.length(n) - 1))

//==============================================================================
export specl_annot.macro '$lhs && $rhs':
  ~order: logical_conjunction
  let lhs_pred = specl_annot_meta.unpack_rhombus_predicate(lhs)
  let rhs_pred = specl_annot_meta.unpack_rhombus_predicate(rhs)
  specl_annot_meta.pack_rhombus_predicate('make_conjunction($lhs_pred, $rhs_pred)')

fun make_conjunction(lhs_pred, rhs_pred):
  fun(target):
    lhs_pred(target) && rhs_pred(target)

//==============================================================================
export specl_annot.macro '$lhs || $rhs':
  ~order: logical_disjunction
  let lhs_pred = specl_annot_meta.unpack_rhombus_predicate(lhs)
  let rhs_pred = specl_annot_meta.unpack_rhombus_predicate(rhs)
  specl_annot_meta.pack_rhombus_predicate('make_disjunction($lhs_pred, $rhs_pred)')

fun make_disjunction(lhs_pred, rhs_pred):
  fun(target):
    lhs_pred(target) || rhs_pred(target)

//==============================================================================
export specl_annot.macro 'matching($(pat :: specl_bind_meta.Parsed))':
  specl_annot_meta.pack_rhombus_predicate(
    'fun(target):
       guard_matching(
         fun():
           $(specl_bind_meta.compile(
               pat,
               'target',
               '#true')))')

fun guard_matching(thunk):
  try:
    thunk()
    ~catch exn :: Exn.Fail.Concretize.Recoverable:
      #false
